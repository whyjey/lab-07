# Введення чисел в одному рядку
numbers = input("Введіть цілі числа, розділені пробілами: ").split()

# Формування рядка з додаванням 'x=>' перед кожним числом
result = ' '.join([f"x=>{num}" for num in numbers])

# Виведення результату
print("Результат:", result)
